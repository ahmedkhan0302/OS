echo -n "Enter the numbers: "
read -a array #reading the array

sorted_arr=($( printf "%s\n" "${array[@]}"|sort -n)) #sorting the array and storing in a new array

size_of_array=${#sorted_arr[@]} # using '#' to get the size of the array

second_greatest=${arr[1]} 
second_smallest=${arr[$s-2]}

difference=$second_greatest-$second_smallest
echo "Difference = $difference"
